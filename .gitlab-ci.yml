image: barbem/risc-v_cep
stages:
  - test

kernel_tests:
     stage: test
     script:
        - timeout 3m make go > report.txt || exit 0
        - grep -q "ERROR" report.txt
        - if [ $? -eq 0 ]; then exit 1; fi
        - grep -q "TESTS REPORT END" report.txt
        - if [ $? -ne 0 ]; then exit 1; fi

